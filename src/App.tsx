import React, { FunctionComponent } from 'react';
import './App.scss';
import Router from './Router';

const App: FunctionComponent = () => {
  return (
    <div className="App">
      <Router />
    </div>
  );
};

export default App;
